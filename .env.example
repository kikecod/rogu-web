# Frontend environment for Vite (development)
# Base URL for backend API (include version path)
VITE_API_BASE_URL=http://localhost:3000/api

# Server URL without /api for static assets
VITE_SERVER_URL=http://localhost:3000

# Controla si el frontend intenta refrescar sesión al iniciar
# 'true' por defecto; usa 'false' para desactivar durante pruebas si el backend aún no setea cookie
VITE_AUTH_REFRESH_ON_START=true
