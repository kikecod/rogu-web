# API Configuration
# URL base de la API del backend (incluye /api)
VITE_API_BASE_URL=http://localhost:3000/api

# URL del servidor (sin /api, para imágenes y archivos estáticos)
VITE_SERVER_URL=http://localhost:3000
